switch_wins = []
user_input = random(1,4)
prize_door = random(1,4)

#need a for loop with a range
def door_selection():
    if user_input != (1,2,3):
        assert ValueError
    else:

def switch_or_stay():
    if user_input != prize_door:
        switch_wins += 1

#switch_wins_percentage = (switch_wins[0]/number of times I run the loop)
